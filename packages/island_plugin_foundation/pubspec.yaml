name: island_plugin_foundation
description: "Reusable JavaScript plugin runtime — discovery, lifecycle, sandbox APIs, and host extension points"
version: 1.0.0
publish_to: none

environment:
  sdk: ">3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_js: ^0.8.7
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  logging: ^1.3.0
  path: ^1.9.1
  path_provider: ^2.1.6
  shared_preferences: ^2.5.5

dev_dependencies:
  build_runner: ^2.16.1
  freezed: ^4.0.2
  json_serializable: ^6.14.1
